{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "dotenv -e .env -- concurrently -k -n api,hrm -c blue,green \"npm run dev --workspace=@workspace/api-server\" \"npm run dev --workspace=@workspace/hrm\"",
    "build": "dotenv -e .env -- npm run build --workspace=@workspace/api-server --workspace=@workspace/hrm",
    "start": "npm run start --workspace=@workspace/api-server",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspace=@workspace/api-server --workspace=@workspace/hrm --workspace=@workspace/mockup-sandbox --workspace=@workspace/scripts"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^10.0.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
